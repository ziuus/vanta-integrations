[package]
name = "iowatch"
version = "0.1.0"
edition = "2021"
description = "Which processes are actually causing disk I/O, and how is that changing?"
license = "MIT"

[lib]
name = "iowatch"
crate-type = ["cdylib", "rlib"]

[dependencies]
extism-pdk = "1.4"
serde_json = "1.0"
vanta-ext-sdk = { path = "../sdk" }
