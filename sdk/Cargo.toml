[package]
name = "vanta-ext-sdk"
version = "0.1.0"
edition = "2021"
description = "Shared SDK for Vanta WASM extensions: host telemetry client, bounded history, and terminal UI primitives"
license = "MIT"

[dependencies]
extism-pdk = "1.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
